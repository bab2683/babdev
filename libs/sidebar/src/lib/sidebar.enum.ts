export enum SidebarStatus {
  OPEN = 'open',
  OPENING = 'opening',
  CLOSE = 'close',
  CLOSING = 'closing'
}

export enum SidebarTouchDirection {
  LEFT = 'left',
  RIGHT = 'right'
}

export enum SidebarPosition {
  LEFT = 'left',
  RIGHT = 'right'
}
